# Contributing

Install dependencies and run the available checks:

~~~bash
npm ci
npm test
npm run build
~~~

Keep geospatial inputs bounded and distinguish seeded scenarios from verified data. Add regression coverage for map rendering, coordinate transforms, exports, and responsive behavior.

Do not commit private coordinates, provider keys, or unverified intelligence as fact.
